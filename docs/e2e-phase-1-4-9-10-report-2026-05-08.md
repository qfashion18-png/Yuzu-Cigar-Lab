# End-to-End Launch Phase Report - 2026-05-08

## Scope

This pass covered Phase 1, Phase 4, Phase 9, and Phase 10 from the production launch runbook:

- Phase 1: local build, lint, test, typecheck, static export, and production dependency audit.
- Phase 4: regulated checkout/compliance behavior for tobacco commerce.
- Phase 9: live AWS infrastructure hardening evidence.
- Phase 10: QA, static preview smoke, security triage, and performance evidence.

## Work Completed

1. Fixed the current production audit blocker.
   - `npm audit --omit=dev` reported a high-severity `fast-uri <=3.1.1` advisory.
   - Ran `npm audit fix`, which moved the installed transitive `fast-uri` dependency to `3.1.2`.
   - Re-ran the full launch gate after the fix; production audit is now clean.

2. Cleaned up fake secret fixtures used by tests.
   - Split fake Stripe-like test strings in source so simple secret scanners do not confuse test fixtures with live credentials.
   - Files changed:
     - `tests/launch-readiness.test.ts`
     - `tests/stripe-commerce.test.ts`

3. Closed the live API Gateway CORS drift.
   - Checked-in API Gateway and Lambda CORS configuration allow `stripe-signature`.
   - Live API Gateway `13710cp67l` now allows `authorization`, `content-type`, `stripe-signature`, and `x-request-id`.
   - Live API Gateway now allows production origins, the Amplify staging origin, and localhost.

## Phase 1 Result: Pass

Command:

```bash
npm run launch:check
```

Result:

- `npm run lint`: passed.
- `npx tsc --noEmit`: passed as part of `launch:check`.
- `npm test`: passed, 213 tests.
- `npm run build`: passed.
- Static export generated 1,076 pages into `out/`.
- `npm audit --omit=dev`: passed, 0 vulnerabilities.

Additional evidence:

- `npm run db:check`: passed through the live API deep health path.
- Direct local TCP to private RDS remains unavailable from this machine, which is expected outside the VPC.

## Phase 4 Result: Code Pass, Provider Gates Still Block Go-Live

Command:

```bash
node --import tsx --test tests/commerce-rules.test.ts tests/checkout-flow.test.ts tests/stripe-commerce.test.ts tests/lambda-ycc-api.test.ts
```

Result:

- 48 targeted compliance, checkout, Lambda, and Stripe tests passed.
- Server-side compliance tests cover:
  - Verified adult checkout acceptance.
  - Rejection of unknown, draft, stale-price, and over-quantity cart lines.
  - Rejection of unverified age, restricted destinations, missing adult signature, unavailable tax, and static placeholder age tokens.
  - Stripe Checkout line-item generation from server-computed values.
  - Membership checkout rejection when server price env is missing.
  - Unsigned Stripe webhook rejection before Cognito auth.

Live-safe API probes:

- `POST /commerce/checkout-session` with a placeholder age token returned HTTP 400.
- `POST /commerce/webhook/stripe` without a Stripe signature returned HTTP 400.
- These probes confirm the live API refuses unsafe checkout/webhook inputs without creating commerce activity.

Remaining Phase 4 blockers:

- Age-verification provider credentials and final decision handling must be confirmed.
- Tobacco tax/excise provider readiness must be confirmed.
- Adult-signature carrier approval must be confirmed.
- Live Stripe provider values must be configured server-side before live payments.

## Phase 9 Result: Partially Pass, External/Infra Gaps Remain

Read-only AWS evidence refreshed on 2026-05-08:

- API Gateway `13710cp67l`:
  - Endpoint is live.
  - `$default` stage has detailed metrics enabled.
  - Default route throttling is burst 100 / rate 50.
  - CORS allows production origins, Amplify staging, and localhost.
  - CORS allows `authorization`, `content-type`, `stripe-signature`, and `x-request-id`.

- Lambda `ycyyy`:
  - State is `Active`.
  - Runtime is `nodejs22.x`.
  - Timeout is 60 seconds, memory is 512 MB.
  - Function is attached to the VPC.
  - Environment has Cognito, RDS Proxy, Bedrock, and schema-write settings.
  - Gap: no live Stripe/provider secrets were visible in Lambda environment.

- RDS `database-1ycc`:
  - Status is `available`.
  - Public access is disabled.
  - Storage is encrypted.
  - Deletion protection is enabled.
  - Backup retention is 7 days.
  - Latest restorable time was current during the check.
  - Enhanced monitoring and Performance Insights are enabled.

- RDS Proxy `proxy-1778040454500-database-1ycc`:
  - Status is `available`.
  - `RequireTLS` is true.
  - Uses Secrets Manager auth for the YCC RDS credential.

- CloudWatch:
  - YCC alarms exist for API 4xx/5xx, Lambda errors/throttles, RDS CPU/storage/connections, and RDS Proxy client connections.
  - Most YCC alarms were OK during the check.
  - RDS Proxy client connections alarm is still `INSUFFICIENT_DATA`.

- SES:
  - Account is healthy, but `ProductionAccessEnabled` is false.
  - SES production access review is denied under case `177809591700724`.
  - This blocks production customer email sends through SES.

- WAF/rate limiting:
  - A regional WAF ACL exists, but it is unrelated and has no API Gateway resources associated.
  - API Gateway throttling is currently the active rate-limit control.
  - Gap: either associate an approved WAF/rule set or explicitly risk-accept API Gateway throttling as the Phase 9 control.

- Secrets Manager:
  - RDS credential exists.
  - No YCC Stripe, age-verification, tax, or shipping/adult-signature provider secret was found by name in the account listing.

Remaining Phase 9 blockers:

1. Obtain SES production access or keep customer email sends disabled.
2. Configure live server-side Stripe/provider secrets in the approved backend secret store.
3. Complete and document a restore drill.
4. Close WAF/rate-limit acceptance.

## Phase 10 Result: QA Pass With Performance Follow-Up

Static preview smoke:

- Served `out/` locally on port 3008.
- HTTP 200:
  - `/`
  - `/shop/`
  - `/shop/curated-padron-1964-anniversary-box/`
  - `/cart/`
  - `/checkout/`
  - `/robots.txt`
  - `/sitemap.xml`
  - A real `/_next/static/...js` asset.

Lighthouse, local static export home page:

- Performance: 56
- Accessibility: 100
- Best Practices: 96
- SEO: 100
- First Contentful Paint: 1.5 s
- Largest Contentful Paint: 8.7 s
- Total Blocking Time: 760 ms
- Cumulative Layout Shift: 0

Security triage:

- `npm audit --omit=dev`: 0 vulnerabilities after the `fast-uri` fix.
- Secret pattern scan found only placeholders, prefix-validation code, the RDS CA bundle, and non-live test fixtures.
- No live-looking Stripe or AWS access key material was found in repo source during this pass.

Browser QA limitation:

- In-app Browser plugin QA was attempted earlier and blocked by `Page.enable` timeouts in the Browser runtime.
- Lighthouse/headless browser and HTTP static smoke checks passed locally, but staging browser QA is still not complete.

Remaining Phase 10 blockers:

1. Run browser smoke on the deployed Amplify staging artifact.
2. Verify mobile navigation, age gate, cart, checkout error handling, account auth, admin handoff, robots, sitemap, and service-worker behavior in a real staging browser.
3. Improve or explicitly accept home-page performance, especially LCP and TBT.

## Strict Go-Live Status

Command:

```bash
npm run launch:go-live-check
```

Result:

- Public/static configuration checks passed.
- Deploy zip shape check passed.
- Strict go-live still fails by design until these gates are confirmed:
  - `NEXT_PUBLIC_REQUIRE_LIVE_AUTH=true`.
  - Backup admin disabled for production.
  - Written Stripe tobacco/cigar commerce approval.
  - Live Stripe secret, webhook secret, portal configuration, and recurring price IDs.
  - Stripe test-mode Checkout and webhook replay.
  - Age verification provider.
  - Tobacco tax/excise provider.
  - Adult-signature carrier approval.
  - AWS restore drill.
  - Staging browser QA.
  - WAF/rate-limit acceptance.

## Current Launch Decision

The repo and static storefront are Phase 1 ready. Phase 4 code behavior is covered and passing. Phase 9 has solid RDS, Lambda, API throttling, metrics, alarms, and CORS foundations, but still has SES denial, missing provider secrets, restore-drill evidence, and WAF/rate-limit acceptance gaps. Phase 10 local QA passes, but staging browser QA and performance follow-up remain open.

Recommendation: do not enable live regulated commerce yet. Continue with static storefront/staging validation, then clear the external provider and AWS gates before switching strict go-live checks to pass.

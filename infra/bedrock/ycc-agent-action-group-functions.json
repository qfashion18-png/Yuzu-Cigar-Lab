{
  "functions": [
    {
      "name": "GetMemberProfile",
      "description": "Return a privacy-minimized membership snapshot from authenticated YCC session attributes.",
      "requireConfirmation": "DISABLED",
      "parameters": {
        "scope": {
          "type": "string",
          "description": "Optional profile scope. Use summary for the default privacy-minimized membership view.",
          "required": false
        }
      }
    },
    {
      "name": "DraftSupportReply",
      "description": "Create an operator-reviewed support reply draft and, when authenticated member context is present, persist a support case and draft email.",
      "requireConfirmation": "DISABLED",
      "parameters": {
        "subject": {
          "type": "string",
          "description": "Short support subject such as renewal charge, damaged shipment, missing item, cancellation, or account question.",
          "required": true
        },
        "message": {
          "type": "string",
          "description": "The member issue or operator notes to summarize into a support draft.",
          "required": true
        },
        "caseId": {
          "type": "string",
          "description": "Optional caller-provided support case correlation ID.",
          "required": false
        }
      }
    },
    {
      "name": "AddHumidorItem",
      "description": "Add a cigar to the authenticated member's humidor inventory using provided cigar details.",
      "requireConfirmation": "ENABLED",
      "parameters": {
        "name": {
          "type": "string",
          "description": "Cigar display name when brand and line are not enough.",
          "required": false
        },
        "brand": {
          "type": "string",
          "description": "Cigar brand, for example Padron or Davidoff.",
          "required": false
        },
        "line": {
          "type": "string",
          "description": "Cigar line, for example 1964 Anniversary or Signature.",
          "required": false
        },
        "vitola": {
          "type": "string",
          "description": "Vitola or shape, for example Toro, Robusto, Churchill, Panetela, or Perfecto.",
          "required": false
        },
        "quantity": {
          "type": "integer",
          "description": "Quantity of cigars to add.",
          "required": false
        }
      }
    },
    {
      "name": "GetAdminQueueSummary",
      "description": "Return safe admin support queue triage guidance for authorized admin or concierge operator sessions.",
      "requireConfirmation": "DISABLED",
      "parameters": {
        "queue": {
          "type": "string",
          "description": "Queue to summarize, such as support, billing, compliance, catalog, or humidor.",
          "required": false
        }
      }
    },
    {
      "name": "UpdateAdminOrder",
      "description": "Apply a confirmed admin order fix for authorized admin or concierge operator sessions by updating status, fulfillmentStatus, or complianceStatus and writing an audit row.",
      "requireConfirmation": "ENABLED",
      "parameters": {
        "orderId": {
          "type": "string",
          "description": "UUID of the commerce order to update.",
          "required": true
        },
        "status": {
          "type": "string",
          "description": "Optional order status such as pending, processing, paid, complete, refunded, failed, or canceled.",
          "required": false
        },
        "fulfillmentStatus": {
          "type": "string",
          "description": "Optional fulfillment status such as not_started, packed, shipped, delivered, fulfilled, blocked, or cancelled.",
          "required": false
        },
        "complianceStatus": {
          "type": "string",
          "description": "Optional compliance status such as pending, verified, review, hold, rejected, cleared, or blocked.",
          "required": false
        }
      }
    },
    {
      "name": "UpdateAdminMemberAccess",
      "description": "Apply a confirmed admin-only user access fix by updating role, memberStatus, or membershipTier and writing an audit row.",
      "requireConfirmation": "ENABLED",
      "parameters": {
        "memberId": {
          "type": "string",
          "description": "UUID of the member record to update.",
          "required": true
        },
        "role": {
          "type": "string",
          "description": "Optional role: customer, operator, or admin.",
          "required": false
        },
        "memberStatus": {
          "type": "string",
          "description": "Optional member status: non_member, active, paused, cancelled, or banned.",
          "required": false
        },
        "membershipTier": {
          "type": "string",
          "description": "Optional membership tier: box_access_pass, kisha, sensei, or daimyo.",
          "required": false
        }
      }
    },
    {
      "name": "DraftWeeklyNews",
      "description": "Create an operator-review-only weekly cigar education/news draft with newsletter copy, source-note placeholders, and compliance review reminders for adult members of legal tobacco age.",
      "requireConfirmation": "DISABLED",
      "parameters": {
        "topic": {
          "type": "string",
          "description": "Topic or theme for the weekly cigar news draft.",
          "required": false
        },
        "timeframe": {
          "type": "string",
          "description": "News window, for example this week or May 2026.",
          "required": false
        },
        "audience": {
          "type": "string",
          "description": "Audience label. Defaults to adult YCC members of legal tobacco age.",
          "required": false
        }
      }
    }
  ]
}
